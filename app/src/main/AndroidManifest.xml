<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reviewandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LayoutActivity.Post" />
        <activity android:name=".LayoutActivity.Profile" />
        <activity android:name=".LayoutActivity.EditProfile" />
        <activity android:name=".LayoutActivity.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LayoutActivity.Register" />
        <activity android:name=".LayoutActivity.Login" />
        <activity android:name=".LayoutActivity.Menu" />
        <activity android:name=".Category.Dapur" />
        <activity android:name=".Category.Tech" />
        <activity android:name=".Category.Otomotif" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>